server {
    listen 80;
    server_name cecisys.com www.cecisys.com;

    location /static/ {
        alias /root/perfil_sensorial/staticfiles/;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/run/gunicorn.sock;
    }
}
